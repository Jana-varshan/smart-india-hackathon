<% layout('layouts/boilerplate') %>
    <h1>All reports </h1>
    <h2 class="text-muted mb-3">Search result on "<%= cve %>"</h2>
        <% for (report of reports) {%>
            <div class="card border border-warning text-light mb-3" style="background-color: black; border-radius: 20px;">
                <div class="row flex-conatiner" >
                    <div class="col-md-4">
                        <img class="img-fluid " style="height: 20rem;object-fit:fill;border-radius: 20px; border:3px solid gray;margin: 1rem 0 1rem 1rem;"  src=<%= report.image %> alt="">
                        
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title mb-3"><%= report.title %></h5>
                            <p class="card-text mb-3"><%= report.description %></p>
                            <p class="card-text mb-3">
                                <small class="text-muted"><%= report.cve %></small>
                            </p>
                            <a href="/allReports/<%= report._id %>" class="btn btn-primary mb-5">View</a>
                        </div>
                    </div>
                </div>
            </div>
        <% }%>

    
        <style>
            .flex-conatiner{
                display: flex;
                flex-direction: row; /* Arrange items horizontally */
                justify-content: space-between; /* Distribute items evenly with space between */
                align-items: center; /* Align items vertically */
            }
        </style>
        